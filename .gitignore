node_modules/
build/
# CAP webDir: only the committed host-picker (index.html) is tracked; the
# rest is generated by `npm run build:cap`.
mobile/webdir/app.html
mobile/webdir/build/
mobile/webdir/styles/
mobile/webdir/assets/
mobile/webdir/manifest.json
mobile/webdir/sw.js
.env
.env.local
.env.openclaw
*.log
# Stub-agent runtime persistence (jsonl transcripts).
agent/data/
backends/stub/data/
# Top-level data/ in case the stub gets booted from repo root.
/data/
sidekick.config.yaml
config.yaml
.DS_Store
__pycache__/
*.pyc
